spring.application.name: approval-system
server:
  port: 8080

spring:
  datasource:
    url: jdbc:mysql://localhost:3306/approval_system?useSSL=false&serverTimezone=UTC
    driver-class-name: com.mysql.cj.jdbc.Driver
    username: root
    password: 000001

flowable:
  database-schema-update: create-drop
  process:
    definition-location-prefix: classpath:/processes/
  rest:
    api:
      enabled: false
